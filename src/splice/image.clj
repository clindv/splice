(ns splice.image
  (:import (java.io File
                    FileFilter)
           (javax.imageio ImageIO))
  (:require [splice.param :as param])
  (:gen-class))
(defn suture [folder images suffix]
  ())
